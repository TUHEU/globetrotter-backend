# --- API Gateway dependencies ---
# fastapi     -> web framework (used here purely for routing + static files)
# uvicorn     -> the server that runs the FastAPI app (the [standard] extra
#                also brings in `websockets`, which the line below pins
#                explicitly anyway since we use it directly - see
#                app/ws_proxy.py)
# httpx       -> makes the actual proxied HTTP calls to the other services
# websockets  -> used to open the Gateway's OUTBOUND connection to Chat
#                Service when proxying a chat WebSocket (app/ws_proxy.py)
# pyjwt       -> ONLY used to gate GET/POST /metrics behind "is this caller
#                an admin?" (see app/security.py) - every other route
#                stays a dumb proxy that never looks inside the token.
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
websockets==13.1
pyjwt==2.9.0

# --- Testing only (not needed to run the service) ---
pytest==8.3.3
