# Configuration for pytest. Having this file means you can simply run
#     python -m pytest
# from the backend folder and it knows where the tests live.
[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
